# Hugging Face Daily Papers 크롤러

이 디렉토리는 Hugging Face Daily Papers를 자동으로 크롤링하여 Jekyll 포스트로 변환하는 스크립트를 포함합니다.

## 주요 기능

- ✅ **일간 논문 수집**: UTC 자정 직후(한국 시간 오전 9시 15분) 자동 크롤링
- ✅ **좋아요 수 기반 정렬**: 수집된 논문을 좋아요 수로 자동 정렬
- ✅ **Abstract 수집**: 각 논문의 상세 페이지에서 Abstract 자동 추출
- ✅ **일간 데이터 저장**: JSON 형식으로 일간 데이터 저장 (`_data/papers/daily-YYYY-MM-DD.json`)
- ✅ **월간 요약 생성**: 일간 데이터를 집계하여 월간 통계 및 Top Papers 생성
- ✅ **자동 포스트 생성**: 일간 및 월간 요약을 Jekyll 포스트로 자동 생성

## 구조

```
scripts/
  └── crawl_hf_papers.py  # 메인 크롤링 스크립트

_data/
  └── papers/
      ├── daily-YYYY-MM-DD.json      # 일간 데이터
      └── monthly-YYYY-MM.json       # 월간 요약

_posts/
  ├── YYYY-MM-DD-daily-papers-summary.md   # 일간 요약 포스트
  └── YYYY-MM-01-monthly-papers-summary.md # 월간 요약 포스트
```

## 사용 방법

### 로컬에서 실행

```bash
# 의존성 설치
pip install -r requirements.txt

# 스크립트 실행
python scripts/crawl_hf_papers.py
```

### GitHub Actions를 통한 자동 실행

`.github/workflows/crawl-papers.yml` 워크플로우가 다음과 같이 설정되어 있습니다:

- **스케줄 실행**: 매일 한국시간 오전 9시 15분 (UTC 00:15)
  - UTC 자정 직후 15분 후에 실행하여 새 날짜 페이지가 확실히 업데이트된 후 크롤링
  - 여유 시간(10-20분)을 두어 롤오버/캐시 지연 대비

- **수동 실행 방법**:
  1. GitHub 저장소의 **Actions** 탭으로 이동
  2. 왼쪽 사이드바에서 **"Crawl Hugging Face Daily Papers"** 워크플로우 선택
  3. 오른쪽의 **"Run workflow"** 버튼 클릭
  4. 브랜치 선택 (보통 `main` 또는 `master`)
  5. **"Run workflow"** 버튼 클릭하여 실행

  또는 GitHub CLI를 사용하는 경우:
  ```bash
  gh workflow run "Crawl Hugging Face Daily Papers.yml"
  ```

## 동작 방식

1. **크롤링**: Hugging Face Papers 페이지와 RSS 피드에서 당일 논문 정보를 가져옵니다.
2. **상세 정보 수집**: 각 논문의 상세 페이지를 방문하여 Abstract, 좋아요 수, 태그 등 추가 정보를 수집합니다.
3. **좋아요 수 정렬**: 수집된 논문을 좋아요 수 기준으로 내림차순 정렬합니다.
4. **일간 데이터 저장**: JSON 형식으로 `_data/papers/daily-YYYY-MM-DD.json`에 저장합니다.
5. **일간 요약 포스트 생성**: 좋아요 순위와 Abstract가 포함된 일간 요약 포스트를 생성합니다.
6. **월간 요약 생성**: 해당 월의 모든 일간 데이터를 집계하여 통계 및 Top Papers를 생성합니다.
7. **자동 커밋**: 변경사항을 자동으로 커밋하고 푸시합니다.

## 데이터 형식

### 일간 데이터 (`daily-YYYY-MM-DD.json`)

```json
{
  "date": "2024-01-15",
  "crawled_at": "2024-01-15T00:15:00",
  "total_papers": 25,
  "papers": [
    {
      "title": "논문 제목",
      "url": "https://huggingface.co/papers/...",
      "abstract": "논문 Abstract...",
      "likes": 123,
      "published": "2024-01-15T00:00:00",
      "paper_link": "https://arxiv.org/abs/...",
      "code_link": "https://github.com/...",
      "tags": ["tag1", "tag2", ...]
    }
  ]
}
```

### 월간 요약 (`monthly-YYYY-MM.json`)

```json
{
  "year": 2024,
  "month": 1,
  "generated_at": "2024-02-01T00:15:00",
  "total_papers": 750,
  "total_likes": 45600,
  "average_likes": 60.8,
  "days_crawled": 31,
  "top_papers": [...],
  "top_tags": [
    {"tag": "deep-learning", "count": 150},
    ...
  ],
  "daily_summaries": [...]
}
```

## 생성되는 포스트 형식

### 일간 요약 포스트

```markdown
---
title: Hugging Face Daily Papers - 2024-01-15
date: 2024-01-15 09:15:00 +0900
categories: [Daily Papers, AI/ML]
tags: [huggingface, papers, daily, ai]
author: lim4349
---

# Hugging Face Daily Papers - 2024-01-15

총 **25개**의 논문이 수집되었습니다.

## 📊 좋아요 순위

1. **논문 제목** - 👍 123
   - [HF 페이지](...)
   - [논문 링크](...)
   - Abstract: ...
```

### 월간 요약 포스트

```markdown
---
title: Hugging Face Papers Monthly Summary - 2024년 1월
date: 2024-01-01 09:00:00 +0900
categories: [Monthly Papers, AI/ML]
tags: [huggingface, papers, monthly, ai, summary]
author: lim4349
---

# Hugging Face Papers 월간 요약 - 2024년 1월

## 📊 통계

- **총 논문 수**: 750개
- **총 좋아요 수**: 45,600
- **평균 좋아요 수**: 60.80
- **수집 일수**: 31일

## 🔥 가장 인기 있는 논문 Top 10

1. **논문 제목** - 👍 450
   ...
```

## 커스터마이징

### 크롤링 타이밍 변경

`.github/workflows/crawl-papers.yml`에서 cron 스케줄 수정:

```yaml
schedule:
  - cron: '15 0 * * *'  # UTC 00:15 = KST 09:15 (권장)
```

다른 시간으로 변경하려면:
- UTC 00:15 → `'15 0 * * *'`
- UTC 00:30 → `'30 0 * * *'`
- 한국 시간 오전 10시 → `'15 1 * * *'` (UTC 01:15)

### 일간 요약에 표시되는 논문 수 변경

`crawl_hf_papers.py`의 `create_daily_summary_post()` 메서드에서:

```python
for i, paper in enumerate(papers[:20], 1):  # 20개로 변경
```

### 월간 요약 생성 시점 변경

`crawl_hf_papers.py`의 `main()` 함수에서:

```python
# 매월 1일에만 생성
if target_date.day == 1:
    summary = crawler.generate_monthly_summary(current_year, current_month)
    crawler.create_monthly_summary_post(summary)

# 또는 매일 생성 (현재 설정)
summary = crawler.generate_monthly_summary(current_year, current_month)
crawler.create_monthly_summary_post(summary)
```

## 참고 사항

- 크롤링은 UTC 자정 직후에 실행되도록 설정되어 있습니다. 이는 Hugging Face Daily Papers가 UTC 자정 기준으로 날짜를 변경하기 때문입니다.
- 서버 부하를 방지하기 위해 각 논문 상세 페이지 요청 간 1초 딜레이가 있습니다.
- Abstract 추출은 페이지 구조에 따라 다를 수 있으므로, 실제 페이지 구조에 맞게 조정이 필요할 수 있습니다.
- 중복 방지: URL 기준으로 중복 논문을 제거합니다.
